# Pack for Claude (if clone UI only shows root README)

Pull branch **xenolift-clean** (now the default). Full tree has `source/`, `ext/`, `docs/`.

This folder mirrors the key planning docs. For real % completion, also open:
- `docs/history/`
- `source/qa/`
- `source/runtime/runtime.c` (large)
- `docs/FIVE_DAY_FINISH.md`
- `docs/MAC_VERIFY_CYCLE1.md`

5-day done bar = continuous Disc1 **field** past CD door — not movie/GPU/portable/launcher.
